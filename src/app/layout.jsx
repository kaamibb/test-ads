import './globals.css'

export const metadata = {
  title: 'Test Ads | Bima Arya Kurniawan',
  description: 'Create next app',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
